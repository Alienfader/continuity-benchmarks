node_modules/
.env
.env.*
!.env.example
reports/tmp/
reports/**/*.checkpoint.json
# middleware-replay pilot outputs are workspace-specific and one-off
reports/middleware-pilot/
*.log
.DS_Store

# Continuity
SESSION_HANDOFF.md
.continuity/

# AI-assistant instruction files (auto-generated locally; not part of the
# benchmark — exclude so contributors' tools don't leak into the repo).
CLAUDE.md
AGENTS.md
GEMINI.md
.cursorrules
.github/copilot-instructions.md

# Local IDE / editor scaffolding
.vscode/

# LongMemEval raw dataset + generated subsamples — regeneratable from the
# subsample script with a fixed seed; not tracked to keep the repo lean.
fixtures/longmemeval/longmemeval_s_cleaned.json
fixtures/longmemeval/longmemeval_m_cleaned.json
fixtures/longmemeval/longmemeval_oracle.json
fixtures/longmemeval/sample-*.json
reports/